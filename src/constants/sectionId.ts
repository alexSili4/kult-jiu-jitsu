const enum SectionId {
  coaches = 'coaches',
  price = 'price',
  amenities = 'amenities',
  veterans = 'veterans',
  contacts = 'contacts',
  programs = 'programs',
  schedule = 'schedule',
}

export default SectionId;
